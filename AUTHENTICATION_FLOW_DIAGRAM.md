# Swift Payment System - Authentication Flow Diagram

## 🔐 Complete Authentication System Flow

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           SWIFT PAYMENT SYSTEM                                  │
│                         Authentication Flow Diagram                             │
└─────────────────────────────────────────────────────────────────────────────────┘

┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   FRONTEND      │    │    BACKEND      │    │   DATABASE      │    │   BROWSER       │
│   (React App)   │    │  (Express API)  │    │  (MongoDB)      │    │ (localStorage)  │
└─────────────────┘    └─────────────────┘    └─────────────────┘    └─────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────┐
│                                1. USER REGISTRATION                            │
└─────────────────────────────────────────────────────────────────────────────────┘

User fills registration form
           │
           ▼
┌─────────────────┐
│ RegisterPage.js │ ────► POST /user/signup
└─────────────────┘           │
                              │
                              ▼
                    ┌─────────────────┐
                    │ user.js         │
                    │ - Validate data │
                    │ - Hash password │
                    │ - Create user   │
                    └─────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │ users Collection│
                    │                 │
                    │ _id: ObjectId   │
                    │ username: "..." │
                    │ full_name: "..."│
                    │ accountNumber   │
                    │ IDNumber: "..." │
                    │ password: hash  │
                    │ userType: "User"│
                    └─────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────┐
│                                2. USER LOGIN                                   │
└─────────────────────────────────────────────────────────────────────────────────┘

User enters credentials
           │
           ▼
┌─────────────────┐
│ LoginPage.js    │ ────► POST /user/login
└─────────────────┘           │
                              │
                              ▼
                    ┌─────────────────┐
                    │ user.js         │
                    │ - Find user     │
                    │ - Verify pass   │
                    │ - Generate JWT  │
                    └─────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │ JWT TOKEN       │
                    │                 │
                    │ Header:         │
                    │ {               │
                    │   "alg": "HS256"│
                    │   "typ": "JWT"  │
                    │ }               │
                    │                 │
                    │ Payload:        │
                    │ {               │
                    │   "id": "..."   │
                    │   "username":   │
                    │   "full_name":  │
                    │   "userType":   │
                    │   "exp": 1h     │
                    │ }               │
                    │                 │
                    │ Signature:      │
                    │ HMACSHA256(     │
                    │   base64(header)│
                    │   + "." +       │
                    │   base64(payload)│
                    │   , secret)     │
                    └─────────────────┘
                              │
                              ▼
┌─────────────────┐    ┌─────────────────┐
│ AuthContext     │ ◄──┤ Login Response  │
│ - setUser()     │    │ {               │
│ - Store in state│    │   token: "..."  │
│                 │    │   user: {...}   │
│                 │    │ }               │
└─────────────────┘    └─────────────────┘
           │
           ▼
┌─────────────────┐
│ localStorage    │
│                 │
│ authToken: "..."│ ◄── Store JWT token
│ currentUser: {...} ◄── Store user data
└─────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────┐
│                           3. PROTECTED ROUTES                                 │
└─────────────────────────────────────────────────────────────────────────────────┘

User navigates to protected page
           │
           ▼
┌─────────────────┐
│ ProtectedRoute  │
│ - Check auth    │
│ - Verify user   │
│ - Role check    │
└─────────────────┘
           │
           ▼
┌─────────────────┐
│ AuthContext     │
│ - isAuthenticated│
│ - isClient()    │
│ - isEmployee()  │
└─────────────────┘
           │
           ▼
┌─────────────────┐
│ localStorage    │
│ - Get token     │
│ - Get user data │
└─────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────┐
│                            4. API CALLS                                       │
└─────────────────────────────────────────────────────────────────────────────────┘

User makes API request
           │
           ▼
┌─────────────────┐
│ apiService.js   │ ────► GET /payments/my-applications
│ - Add token     │           │
│ - Headers:      │           │
│   Authorization │           │
│   Bearer token  │           │
└─────────────────┘           │
                              │
                              ▼
                    ┌─────────────────┐
                    │ check-auth.js   │
                    │ - Extract token │
                    │ - Verify JWT    │
                    │ - Set req.user  │
                    └─────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │ payments.js     │
                    │ - req.user.id   │
                    │ - Query DB      │
                    │ - Return data   │
                    └─────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │ payment_applications│
                    │ Collection      │
                    │                 │
                    │ _id: ObjectId   │
                    │ recipientName   │
                    │ accountNumber   │
                    │ swiftCode       │
                    │ amount: Number  │
                    │ currency        │
                    │ paymentProvider │
                    │ submittedBy: ID │
                    │ submittedByName │
                    │ status: "pending"│
                    │ submittedAt     │
                    │ reviewedAt      │
                    │ reviewedBy      │
                    │ reviewerName    │
                    │ reviewComments  │
                    └─────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────┐
│                              5. LOGOUT                                         │
└─────────────────────────────────────────────────────────────────────────────────┘

User clicks logout
           │
           ▼
┌─────────────────┐
│ LogoutModal     │
│ - Confirmation  │
│ - User confirms │
└─────────────────┘
           │
           ▼
┌─────────────────┐
│ AuthContext     │
│ - logout()      │
│ - setUser(null) │
└─────────────────┘
           │
           ▼
┌─────────────────┐
│ apiService.js   │
│ - logout()      │
│ - Clear storage │
└─────────────────┘
           │
           ▼
┌─────────────────┐
│ localStorage    │
│                 │
│ authToken: null │ ◄── Clear token
│ currentUser: null ◄── Clear user data
│ userPreferences: null
│ lastLoginTime: null
└─────────────────┘
           │
           ▼
┌─────────────────┐
│ Redirect to     │
│ Login Page      │
└─────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────┐
│                            DATABASE SCHEMAS                                    │
└─────────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────┐
│                              USERS COLLECTION                                 │
└─────────────────────────────────────────────────────────────────────────────────┘

{
  "_id": ObjectId("..."),
  "username": "john_doe",
  "full_name": "John Doe",
  "accountNumber": "123456789",
  "IDNumber": "1234567890123",
  "password": "$2b$10$hashedpassword...",
  "userType": "User"  // or "Employee"
}

┌─────────────────────────────────────────────────────────────────────────────────┐
│                        PAYMENT_APPLICATIONS COLLECTION                         │
└─────────────────────────────────────────────────────────────────────────────────┘

{
  "_id": ObjectId("..."),
  "recipientName": "Jane Smith",
  "accountNumber": "987654321",
  "swiftCode": "ABZAZAJJ",
  "amount": 5000.00,
  "currency": "ZAR",
  "paymentProvider": "Bank Transfer",
  "submittedBy": ObjectId("user_id"),
  "submittedByName": "John Doe",
  "status": "pending",  // pending, approved, rejected
  "submittedAt": ISODate("2025-01-08T10:30:00Z"),
  "reviewedAt": null,
  "reviewedBy": null,
  "reviewerName": null,
  "reviewComments": null
}

┌─────────────────────────────────────────────────────────────────────────────────┐
│                              KEY COMPONENTS                                    │
└─────────────────────────────────────────────────────────────────────────────────┘

🔐 JWT TOKEN STRUCTURE:
├── Header: Algorithm & Type
├── Payload: User data + expiration
└── Signature: HMAC SHA256 with secret

🗄️ LOCALSTORAGE DATA:
├── authToken: JWT string
├── currentUser: User object (JSON)
├── userPreferences: Settings (optional)
└── lastLoginTime: Timestamp (optional)

🛡️ AUTHENTICATION FLOW:
├── 1. User submits credentials
├── 2. Backend validates & generates JWT
├── 3. Frontend stores token & user data
├── 4. Subsequent requests include token
├── 5. Backend verifies token on each request
└── 6. Logout clears all stored data

🔒 SECURITY FEATURES:
├── Password hashing with bcrypt
├── JWT signature verification
├── Token expiration (1 hour)
├── Role-based access control
└── Secure HTTPS communication

┌─────────────────────────────────────────────────────────────────────────────────┐
│                              API ENDPOINTS                                     │
└─────────────────────────────────────────────────────────────────────────────────┘

🔐 AUTHENTICATION:
├── POST /user/signup - Create new user
└── POST /user/login - Authenticate user

💳 PAYMENTS (Protected):
├── POST /payments/submit - Submit payment
├── GET /payments/my-applications - User's payments
├── GET /payments/all - All payments (Employee)
├── GET /payments/:id - Specific payment
├── PATCH /payments/review/:id - Review payment
└── GET /payments/status/:status - Filter by status

┌─────────────────────────────────────────────────────────────────────────────────┐
│                              USER TYPES                                        │
└─────────────────────────────────────────────────────────────────────────────────┘

👤 CLIENT (userType: "User"):
├── Can submit payments
├── Can view own transactions
├── Can view account info
└── Cannot review other payments

👨‍💼 EMPLOYEE (userType: "Employee"):
├── Can view all payments
├── Can approve/reject payments
├── Can access employee dashboard
└── Cannot submit payments

┌─────────────────────────────────────────────────────────────────────────────────┐
│                              ROLE-BASED ROUTES                                │
└─────────────────────────────────────────────────────────────────────────────────┘

🔒 CLIENT ROUTES:
├── /payment - Submit payment form
├── /transactions - View transaction history
├── /account-info - View account details
└── /ClientHome - Client dashboard

👨‍💼 EMPLOYEE ROUTES:
├── /pendingPayments - Review payments
├── /EmployeeHome - Employee dashboard
└── Cannot access client-specific routes

🌐 PUBLIC ROUTES:
├── / - Login page
└── /register - Registration page

┌─────────────────────────────────────────────────────────────────────────────────┐
│                              SECURITY MIDDLEWARE                              │
└─────────────────────────────────────────────────────────────────────────────────┘

🛡️ CHECK-AUTH MIDDLEWARE:
├── Extracts token from Authorization header
├── Verifies JWT signature with secret
├── Checks token expiration
├── Sets req.user with decoded data
└── Allows/denies request based on validity

🔐 PROTECTED ROUTE COMPONENT:
├── Checks authentication status
├── Verifies user role requirements
├── Redirects to login if unauthorized
└── Renders protected content if authorized

┌─────────────────────────────────────────────────────────────────────────────────┐
│                              ERROR HANDLING                                    │
└─────────────────────────────────────────────────────────────────────────────────┘

❌ AUTHENTICATION ERRORS:
├── Invalid credentials
├── Expired token
├── Missing token
├── Invalid signature
└── Insufficient permissions

🔄 ERROR RESPONSES:
├── 401 Unauthorized - Invalid/missing token
├── 403 Forbidden - Valid token, insufficient permissions
├── 400 Bad Request - Invalid request data
└── 500 Internal Server Error - Server issues

┌─────────────────────────────────────────────────────────────────────────────────┐
│                              SESSION MANAGEMENT                                │
└─────────────────────────────────────────────────────────────────────────────────┘

⏰ TOKEN LIFECYCLE:
├── Created: On successful login
├── Valid: 1 hour from creation
├── Used: On every API request
├── Refreshed: Not implemented (would require refresh token)
└── Destroyed: On logout or expiration

🔄 SESSION PERSISTENCE:
├── localStorage persists across browser sessions
├── User stays logged in after closing browser
├── Token must be valid and not expired
└── Manual logout required to end session

┌─────────────────────────────────────────────────────────────────────────────────┐
│                              DEVELOPMENT NOTES                                 │
└─────────────────────────────────────────────────────────────────────────────────┘

🔧 CURRENT LIMITATIONS:
├── No refresh token mechanism
├── No password reset functionality
├── No email verification
├── No account lockout after failed attempts
└── No admin panel for user management

🚀 POTENTIAL IMPROVEMENTS:
├── Implement refresh tokens
├── Add password reset flow
├── Add email verification
├── Implement rate limiting
├── Add audit logging
└── Create admin management panel

┌─────────────────────────────────────────────────────────────────────────────────┐
│                              TESTING SCENARIOS                                 │
└─────────────────────────────────────────────────────────────────────────────────┘

✅ HAPPY PATH:
├── User registers successfully
├── User logs in with valid credentials
├── JWT token is created and stored
├── User accesses protected routes
├── API calls include valid token
└── User logs out successfully

❌ ERROR SCENARIOS:
├── Registration with existing username
├── Login with invalid credentials
├── Accessing protected route without token
├── Using expired token
├── Accessing unauthorized route
└── Token tampering attempts

┌─────────────────────────────────────────────────────────────────────────────────┐
│                              END OF DIAGRAM                                   │
└─────────────────────────────────────────────────────────────────────────────────┘
```

## 📋 Summary

This diagram shows the complete authentication flow of the Swift Payment System, including:

1. **User Registration** - How new users are created and stored
2. **User Login** - JWT token generation and storage
3. **Protected Routes** - Authentication and authorization checks
4. **API Calls** - Token verification and data retrieval
5. **Logout** - Session cleanup and data clearing
6. **Database Schemas** - User and payment application structures
7. **Security Features** - JWT signing, password hashing, role-based access
8. **Error Handling** - Various failure scenarios and responses

The system uses a combination of JWT tokens, localStorage, and role-based access control to provide secure authentication and authorization throughout the application.
